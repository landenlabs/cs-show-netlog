<Window x:Class="ShowNetLog.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="300" Width="400" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="NetLimiter Log Viewer" FontSize="18" FontWeight="Bold" Margin="0,0,0,5"/>
        <TextBlock x:Name="VersionText" Grid.Row="1" Text="Version: 1.0.0" Margin="0,0,0,2"/>
        <TextBlock x:Name="BuildDateText" Grid.Row="2" Text="Build Date: 2026-06-06" Margin="0,0,0,15"/>

        <GroupBox Header="Preferences" Grid.Row="3" Padding="10">
            <StackPanel>
                <CheckBox x:Name="AllColumnsCheckBox" Content="Show All Columns (Local IP/Domain)" IsChecked="False" Click="AllColumnsCheckBox_Click"/>
            </StackPanel>
        </GroupBox>

        <Button Content="Close" Grid.Row="4" Width="80" HorizontalAlignment="Right" Click="CloseButton_Click" Margin="0,10,0,0"/>
    </Grid>
</Window>
