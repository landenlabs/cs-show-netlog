<!-- Copyright (c) 2026 LanDen Labs - Dennis Lang -->
<Window x:Class="ShowNetLog.SummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Traffic Summary" Height="450" Width="500"
        WindowStartupLocation="CenterOwner"
        Icon="/screens/icon.png">
    <Grid Margin="10">
        <TabControl>
            <TabItem Header="Domains">
                <DataGrid x:Name="DomainsDataGrid" AutoGenerateColumns="False" IsReadOnly="True" AlternatingRowBackground="#F0F0F0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Domain" Binding="{Binding Key}" Width="*"/>
                        <DataGridTextColumn Header="Count" Binding="{Binding Count}" Width="80"/>
                        <DataGridTextColumn Header="Total Duration (s)" Binding="{Binding TotalDuration}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="RPort">
                <DataGrid x:Name="PortsDataGrid" AutoGenerateColumns="False" IsReadOnly="True" AlternatingRowBackground="#F0F0F0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Port" Binding="{Binding Key}" Width="*"/>
                        <DataGridTextColumn Header="Count" Binding="{Binding Count}" Width="80"/>
                        <DataGridTextColumn Header="Total Duration (s)" Binding="{Binding TotalDuration}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
